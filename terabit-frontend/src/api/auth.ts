export async function login(email: string, password: string): Promise<void> {
  // TODO: trocar por chamada real ao servidor OAuth2/OIDC
  // por enquanto, simula sucesso sempre
  return Promise.resolve();
}